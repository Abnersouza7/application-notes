# mask-fingerprint: 712e82a12f7b
lsm6dsv at <addr>, adapter rates PUSH_PULL_5_MHZ_50_DC / OPEN_DRAIN_1_MHZ at 3300 mV

  feature                     verdict           evidence
  --------------------------  ----------------  --------
  GETPID                      supported         02 08 00 70 12 0B, device id 0x0070
  GETBCR                      supported         0x07
  GETDCR                      supported         0x44
  control: refusal reporting  supported         GETPID, SETMWL and a private read at the unoccupied address <addr> were all refused, so a SUCCESS above means the target accepted the command
  SETMWL / GETMWL             supported         8 then set 64 then 64
  SETMRL / GETMRL             supported         16 then set 32 then 32, maximum IBI payload 10 throughout
  SETGRPA / RSTGRPA           supported         write to <addr> through 0x20: before refused, assigned landed, released refused
  read at the group address   not implemented   refused while group addressing was working, which is correct and is why a read is not a valid test
  HDR modes                   not implemented   BCR bit 5 clear, so the target declares SDR only
  GETHDRCAP (0x95)            undetermined      answers 00 11 18 00
  HDR-DDR read attempt        undetermined      returned SUCCESS, which is not diagnostic; bus intact afterwards
  ENTAS0                      undetermined      accepted <n> of <m> attempts; proving an activity state needs a current measurement
  ENTAS1                      undetermined      refused <n> of <m> attempts, so the target does report this one, though not on every attempt
  ENTAS2                      undetermined      refused <n> of <m> attempts, so the target does report this one, though not on every attempt
  ENTAS3                      undetermined      refused <n> of <m> attempts, so the target does report this one, though not on every attempt
  RSTACT                      undetermined      returned SUCCESS; no observable effect from the host side
  GETMXDS                     undetermined      answers 08 60; a part that implements it reports real limits
  IBI payload negotiation     supported         the target declared 10 byte(s), the adapter takes 8, so it was asked for 8; without this every interrupt is discarded and nothing reports it
  IBI before ENEC             not implemented   <n> in <seconds>
  ENEC and IBI delivery       supported         <n> in <seconds> = <rate>, against a configured 60 Hz (<error>)
  IBI mandatory data byte     supported         0x02: data ready=True, wake-up or basic interrupt function=False, raw=0x02; followed by 7 further byte(s) 00 00 00 05 04 00 00, SLEEP_CHANGE=0, D6D=0, TAP=0, WU=0, FF=0
  IBI payloads seen           supported         {<payloads>: <n>}
  DISEC stops them            supported         the stream stopped after <n> DISEC attempts
  hot-join                    not implemented   no hot-join notification at any point
  SETXTIME / GETXTIME         supported         <before> then <after> after SETXTIME <subcommand>; timing control disabled again afterwards
  SETNEWDA                    supported         moved 0x08 to <addr> and the old address stopped answering
  RSTDAA                      supported         the table dropped to [<addrs>] and 0x0A stopped answering
  the part was reset afterwards, to undo the modes this battery latched
  <counts>
  a verdict of undetermined means the command completed and nothing measurable changed.
  it is not a no: some targets accept and discard commands they do not implement
  and some refuse them, and which one a target does is a property of that target.
  So a success alone is never evidence of support, because a refusal is not guaranteed to arrive.
